[{"filename": "lib/Dialect/TritonGPU/Transforms/Utility.cpp", "status": "modified", "additions": 4, "deletions": 2, "changes": 6, "file_content_changes": "@@ -215,8 +215,10 @@ Operation *cloneWithInferType(mlir::OpBuilder &rewriter, Operation *op,\n         newOp->getContext(), newOp->getLoc(), newOp->getOperands(),\n         newOp->getAttrDictionary(), newOp->getPropertiesStorage(),\n         newOp->getRegions(), newTypes);\n-    if (succeeded(success))\n-      newOp->getResult(0).setType(newTypes.front());\n+    if (succeeded(success)) {\n+      for (size_t i = 0; i < newTypes.size(); i++)\n+        newOp->getResult(i).setType(newTypes[i]);\n+    }\n   }\n   return newOp;\n }"}]