[{"filename": "include/triton/Dialect/TritonGPU/IR/TritonGPUAttrDefs.td", "status": "modified", "additions": 4, "deletions": 3, "changes": 7, "file_content_changes": "@@ -94,13 +94,14 @@ A_{3, 2}  A_{3, 3}  A_{3, 0}  A_{3, 1} ...   [phase 1] /\n         // ---- begin version 1 ----\n         if (version == 1) {\n           bool is_row = order[0] != 0;\n-          bool is_vec4 = opIdx == 0 ? is_row && (shape[order[0]] <= 16) :\n-                                      !is_row && (shape[order[0]] <= 16);\n+          bool is_vec4 = opIdx == 0 ? !is_row && (shape[order[0]] <= 16) :\n+              is_row && (shape[order[0]] <= 16);\n           int pack_size = opIdx == 0 ? ((is_row || is_vec4) ? 1 : 2) :\n                                        ((is_row && !is_vec4) ? 2 : 1);\n           int rep = 2 * pack_size;\n           int maxPhase = (order[inner] == 1 ? 8 : 4) / perPhase;\n-          return $_get(context, 2 * rep, perPhase, maxPhase, order);\n+          int vec = 2 * rep;\n+          return $_get(context, vec, perPhase, maxPhase, order);\n         }\n \n         // ---- begin version 2 ----"}]