[{"filename": ".github/workflows/integration-tests.yml", "status": "modified", "additions": 3, "deletions": 0, "changes": 3, "file_content_changes": "@@ -83,6 +83,9 @@ jobs:\n         if: ${{ env.BACKEND == 'CUDA'}}\n         run: |\n           cd python/test/unit\n+          # warm up the cache\n+          python3 -m pytest --warmup -n 64 --ignore=runtime\n+          # run the tests\n           python3 -m pytest -n 8 --ignore=runtime\n           # run runtime tests serially to avoid race condition with cache handling.\n           python3 -m pytest runtime/"}, {"filename": "python/test/unit/conftest.py", "status": "added", "additions": 17, "deletions": 0, "changes": 17, "file_content_changes": "@@ -0,0 +1,17 @@\n+# content of conftest.py\n+\n+import pytest\n+\n+\n+def pytest_addoption(parser):\n+    parser.addoption(\"--warmup\", action=\"store_true\", default=False, help=\"Ignore test failures\")\n+\n+\n+@pytest.hookimpl(hookwrapper=True)\n+def pytest_runtest_makereport(item, call):\n+    outcome = yield\n+    report = outcome.get_result()\n+    # Make all the tests pass during warmup\n+    if item.config.getoption(\"--warmup\"):\n+        report.outcome = \"passed\"\n+        report.longrepr = None"}]