[{"filename": "include/triton/Dialect/TritonGPU/IR/Dialect.h", "status": "modified", "additions": 1, "deletions": 2, "changes": 3, "file_content_changes": "@@ -80,10 +80,9 @@ bool isaDistributedLayout(Attribute layout);\n \n bool isSharedEncoding(Value value);\n \n-} // namespace gpu\n-\n bool isExpensiveCat(CatOp cat, Attribute &targetEncoding);\n \n+} // namespace gpu\n } // namespace triton\n } // namespace mlir\n "}, {"filename": "lib/Dialect/TritonGPU/IR/Dialect.cpp", "status": "modified", "additions": 1, "deletions": 2, "changes": 3, "file_content_changes": "@@ -367,8 +367,6 @@ bool isSharedEncoding(Value value) {\n   return false;\n }\n \n-} // namespace gpu\n-\n bool isExpensiveCat(CatOp cat, Attribute &targetEncoding) {\n   // If the new elements per thread is less than the old one, we will need to do\n   // convert encoding that goes through shared memory anyway. So we consider it\n@@ -382,6 +380,7 @@ bool isExpensiveCat(CatOp cat, Attribute &targetEncoding) {\n   return newTotalElemsPerThread < totalElemsPerThread;\n }\n \n+} // namespace gpu\n } // namespace triton\n } // namespace mlir\n "}]