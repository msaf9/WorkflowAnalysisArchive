[{"filename": "lib/Analysis/Membar.cpp", "status": "modified", "additions": 2, "deletions": 2, "changes": 4, "file_content_changes": "@@ -83,8 +83,8 @@ void MembarAnalysis::transfer(Operation *op, RegionInfo *regionInfo,\n \n   if (isa<triton::gpu::AsyncWaitOp>(op) &&\n       !isa<gpu::BarrierOp>(op->getNextNode())) {\n-    // If the current op is an async wait, we insert a barrier op and sync\n-    // if the next op is not a barrier\n+    // If the current op is an async wait and the next op is not a barrier we\n+    // insert a barrier op and sync\n     regionInfo->sync();\n     OpBuilder::InsertionGuard g(*builder);\n     builder->setInsertionPointAfter(op);"}]