[{"filename": "python/triton/runtime/autotuner.py", "status": "modified", "additions": 2, "deletions": 2, "changes": 4, "file_content_changes": "@@ -185,11 +185,11 @@ def kernel(x_ptr, x_size, **META):\n     .type values: dict[str, Callable[[list[Any]], Any]]\n     \"\"\"\n     def decorator(fn):\n+        old_run = fn.run\n         def new_run(*args, **meta):\n             for v, heur in values.items():\n-                assert v not in meta\n                 meta[v] = heur({**dict(zip(fn.arg_names, args)), **meta})\n-            return fn.run(*args, **meta)\n+            return old_run(*args, **meta)\n         fn.run = new_run\n         return fn\n "}]