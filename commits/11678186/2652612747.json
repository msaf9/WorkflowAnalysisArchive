[{"filename": "lib/codegen/analysis/layout.cc", "status": "modified", "additions": 1, "deletions": 0, "changes": 1, "file_content_changes": "@@ -242,6 +242,7 @@ mma_layout::mma_layout(size_t num_warps,\n       if((dot && dot->get_operand(2)!=v) || !layout_a->to_shared() || cts)\n         one_warp_per_row = shape[0] / spw_[0] >= num_warps;\n     }\n+    // std::cout << one_warp_per_row << std::endl;\n \n     if(one_warp_per_row){\n       wpt_[1] = 1;"}, {"filename": "lib/codegen/selection/generator.cc", "status": "modified", "additions": 0, "deletions": 2, "changes": 2, "file_content_changes": "@@ -2868,8 +2868,6 @@ void generator::visit_layout_convert(ir::value *out, ir::value *in){\n   int pad = std::max(in_vec, out_vec);\n   Value *in_ld = i32(shape[in_ord[0]] + pad);\n   Value *out_ld = i32(shape[out_ord[0]] + pad);\n-  // if(in_layout->to_mma())\n-  //   std::cout << in->get_type()->get_scalar_ty()->is_fp32_ty() << \" \" << in_layout->to_mma()->wpt(1) << std::endl;\n   for(int i = 0; i < n_reps[0]; i++)\n   for(int j = 0; j < n_reps[1]; j++){\n     int max_ii, max_jj;"}]