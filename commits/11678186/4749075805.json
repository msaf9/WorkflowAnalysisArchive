[{"filename": "python/triton/compiler/code_generator.py", "status": "modified", "additions": 0, "deletions": 1, "changes": 1, "file_content_changes": "@@ -750,7 +750,6 @@ def visit_keyword(self, node) -> Tuple[str, Any]:\n     def visit_Assert(self, node) -> Any:\n         if not self.debug:\n             return\n-        print(\"AA\")\n         test = self.visit(node.test)\n         msg = self.visit(node.msg)\n         # Convert assert to triton's device_assert which happens on the device"}]