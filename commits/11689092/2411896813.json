[{"filename": "python/triton/code_gen.py", "status": "modified", "additions": 6, "deletions": 1, "changes": 7, "file_content_changes": "@@ -1150,6 +1150,11 @@ def visit_Call(self, node):\n         self.ret = hashlib.md5(self.ret).hexdigest()\n \n \n+def default_cache_dir():\n+    import getpass\n+    return f'/tmp/triton_{getpass.getuser()}'\n+\n+\n class JITFunction:\n \n     cache_hook = None\n@@ -1235,7 +1240,7 @@ def _warmup(self, key, arg_types, device, attributes, constants, num_warps, num_\n         hashed_key = hashlib.md5(key.encode(\"utf-8\")).hexdigest()\n \n         # create cache directory\n-        cache_dir = os.environ.get('TRITON_CACHE_DIR', '/tmp/triton/')\n+        cache_dir = os.environ.get('TRITON_CACHE_DIR', default_cache_dir())\n         if cache_dir:\n             os.makedirs(cache_dir, exist_ok=True)\n "}]